# ==========================================
# PagePulse Environment Variables Template
# ==========================================

# ------------------------------------------
# Backend Config (FastAPI)
# ------------------------------------------
PORT=8000
HOST=127.0.0.1
LOG_LEVEL=INFO

# ------------------------------------------
# Frontend Config (React / Vite)
# ------------------------------------------
# URL pointing to the running backend service.
# Use http://localhost:8000 for local runs.
VITE_API_URL=http://localhost:8000
